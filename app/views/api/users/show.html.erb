<p><%= current_user.id %></p>
<p><%= current_user.username %></p>
<p><%= current_user.session_token %></p>
<p><%= current_user.endorsements %></p>
<p><%= current_user.cookbook %></p>
<h1>Destroy</h1>
<%= link_to "delete", api_user_url, method: :delete,
                                  data: { confirm: "You sure?" } %>
<%= form_for :user, :url => api_user_url, :html => {:method => :delete} do |f| %>
	<%= f.label :username %>
	<%= f.text_field :username %>
	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.submit %>
<% end %>

<%= link_to "Cookbook", controller: "cookbooks", action: "show", user_id: current_user.id, id: current_user.cookbook.id %>